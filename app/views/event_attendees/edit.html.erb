<h3>Update attendee..</h3>
<%= form_for @attendee , :url => event_attendee_path(@event,@attendee) , :html => {:method => :put} do |f| %>
   <%= f.text_field :name %>
   <%= f.submit "submit" %>
<% end %>
